document.querySelector(".carousel").addEventListener('slide.bs.carousel', fadeSlide);

function fadeSlide() {
    let slideList = document.querySelectorAll('.carousel-caption');
    
    // let slideArray = Array.from(slideList);
    // console.log(slideArray[0])
}